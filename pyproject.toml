[build-system]
requires = ["setuptools>=64", "wheel", "pybind11>=2.11"]
build-backend = "setuptools.build_meta"

[project]
name = "aynime_capture"
version = "0.1.0"
requires-python = ">=3.9"
description = "Windows desktop capture library (Windows only)"
